// ((******* SIGNUP ********))
export const signup = async (req, res) => {

    res.json({ data: "Your hit the signup end point" })
}

// ((*******Login*********))
export const login = async (req, res) => {

    res.json({ data: "Your hit the login end point" })

}
// ((***** Logout *****))
export const logout = async (req, res) => {
    res.json({ data: "Your hit the logout end point" })
}